<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Toolbox.Pages.AudioSplitterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Audio Splitter" />
            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Split one audio file into two equal parts." />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Padding="20"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel Spacing="18">
                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Audio file" />
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox
                                x:Name="AudioPathBox"
                                IsReadOnly="True"
                                PlaceholderText="No file selected" />
                            <Button
                                x:Name="BrowseAudioButton"
                                Grid.Column="1"
                                Click="BrowseAudioButton_Click">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon FontSize="14" Glyph="&#xE8E5;" />
                                    <TextBlock Text="Browse" />
                                </StackPanel>
                            </Button>
                        </Grid>
                    </StackPanel>

                    <Button
                        x:Name="SplitAudioButton"
                        HorizontalAlignment="Left"
                        Click="SplitAudioButton_Click"
                        IsEnabled="False"
                        Style="{StaticResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon FontSize="14" Glyph="&#xE768;" />
                            <TextBlock Text="Split audio" />
                        </StackPanel>
                    </Button>

                    <StackPanel Spacing="8">
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Progress" />
                        <ProgressBar
                            x:Name="AudioProgressBar"
                            Maximum="100"
                            Minimum="0"
                            Value="0" />
                        <TextBlock
                            x:Name="AudioStatusText"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Ready" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border
                Grid.Column="1"
                Padding="20"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel Spacing="14">
                    <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Output" />
                    <TextBlock
                        x:Name="AudioOutputText"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="Files will appear next to the source audio."
                        TextWrapping="WrapWholeWords" />
                    <InfoBar
                        x:Name="AudioInfoBar"
                        IsOpen="False"
                        Severity="Informational" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
